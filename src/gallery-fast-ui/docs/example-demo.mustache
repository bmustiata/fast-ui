<div class="intro">
    <p>
    *Tis' but a demo page*
    </p>
</div>

<h2>Description</h2>

<p>*Module Description Goes Here*</p>

<div id="demo" class="example yui3-skin-sam">
	<script>
        var htmlCode = '' +
                '<Y:Panel ui:field="dialog" ui:srcNode="div" xmlns:Y="Y" xmlns:ui="fastui" headerContent="awesome title">' +
                '    <ui:config name="width">400</ui:config>' +
                '    <ui:config name="centered">true</ui:config>' +
                '    <ui:config name="modal">false</ui:config>' +
                '    <ui:config name="visible">false</ui:config>' +
                '' +
                '    <ui:config name="bodyContent" type="ui">' +
                '        <Y:TabView ui:srcNode="div">' +
                '            <ul>' +
                '                <li><a href="tab1">{HOME}</a></li>' +
                '                <li><a href="tab2">{OTHER}</a></li>' +
                '            </ul>' +
                '            <div>' +
                '                <div id="tab1">' +
                '                    <div>' +
                '                    {SOME_MESSAGE}' +
                '                    </div>' +
                '                    <Y:Button ui:srcNode="button" ui:field="okButton" label="{OK}"/>' +
                '                    <Y:Button ui:srcNode="button" ui:field="cancelButton" label="{CANCEL}"/>' +
                '                </div>' +
                '                <div id="tab2">' +
                '                    <Y:Button ui:srcNode="button" ui:field="otherButton" label="{OTHER_BUTTON}"/>' +
                '                </div>' +
                '            </div>' +
                '        </Y:TabView>' +
                '    </ui:config>' +
                '</Y:Panel>';


		YUI({
			modules:{
				'gallery-fast-ui': {
					fullpath: '/build/gallery-fast-ui/gallery-fast-ui.js'
				}
			}
	    }).use('{{use}}', 'button', 'tabview', 'panel', function(Y){
		    var msg = {
		        HOME : "Home",
		        OK : "Ok",
		        CANCEL : "Cancel",
		        SOME_MESSAGE : "Some message",
		        OTHER : "Other",
		        OTHER_BUTTON : "Other Button"
		    };

            // we render it in the "#demo" div just for the skin.
		    var ui = Y.fastUi("#demo", htmlCode, msg);

            ui.okButton.on("click", function() {
                alert('ok button was clicked');
            });
            ui.cancelButton.on("click", function() {
                ui.dialog.hide();
            });


            Y.fastUi("#demo", "<Y:Button ui:field='demoButton' label='show dialog' xmlns:Y='Y' xmlns:ui='fastui'/>")
		        .demoButton.on("click", function() {
                    ui.dialog.show();
		        });


		});
	</script>
</div>